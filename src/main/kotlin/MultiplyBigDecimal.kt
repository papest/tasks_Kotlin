/**
 * Multiplication
 * Write a program that takes two strings as input, converts them to BigDecimal numbers, multiplies them, and prints the result.
 */

fun main() {
    println(readln().toBigDecimal().multiply(readln().toBigDecimal()))
}